import { README_DISPLAY_TYPES, README_FAVORITES_TYPES } from "../../types/envTypes";

function filterFavoritesReadme(
  animeData: any[],
  mangaData: any[],
  characterData: any[],
  peopleData: any[],
  readmeFavorites: README_FAVORITES_TYPES,
  readmeDisplay: README_DISPLAY_TYPES
) {
  var readmeNewData = [];
}
